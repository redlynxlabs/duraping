{
  "schemaVersion": 1,
  "id": "${modid}",
  "version": "${version}",
  "name": "${name}",
  "description": "Lightweight durability alerts with sound/chat/toast/flash. Client-only QoL.",
  "authors": ["redkey"],
  "contact": {
    "sources": "https://github.com/redkey-labs/duraping"
  },
  "license": "MIT",
  "icon": "assets/duraping/icon.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "sh.redkey.mc.duraping.DuraPingClient"
    ],
    "modmenu": [
      "sh.redkey.mc.duraping.config.ModMenuIntegration"
    ]
  },
  "depends": {
    "fabricloader": ">=0.17.3",
    "fabric": "*",
    "minecraft": "1.21.9",
    "cloth-config2": ">=15.0.0"
  },
  "mixins": [
    "duraping.mixins.json"
  ]
}

